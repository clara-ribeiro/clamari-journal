import { HomeScreen, homeMetadata } from "@/lib/locale-screens";

export const metadata = homeMetadata("pt-BR");

export default function PortugueseHomePage() {
  return <HomeScreen locale="pt-BR" />;
}
