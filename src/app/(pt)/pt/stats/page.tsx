import { StatsScreen, statsPageMetadata } from "@/lib/locale-screens";

export const metadata = statsPageMetadata("pt-BR");

export default function PortugueseStatsPage() {
  return <StatsScreen locale="pt-BR" />;
}
