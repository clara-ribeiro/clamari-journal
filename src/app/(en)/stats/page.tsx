import { StatsScreen, statsPageMetadata } from "@/lib/locale-screens";

export const metadata = statsPageMetadata("en");

export default function StatsPage() {
  return <StatsScreen locale="en" />;
}
