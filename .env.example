TMDB_ACCESS_TOKEN=
# Optional ISO language for TMDB search/detail (default: en-US), e.g. pt-BR
TMDB_LANGUAGE=
GOOGLE_BOOKS_API_KEY=
# Canonical origin for metadataBase / sitemap / robots (no trailing slash).
# Optional on Vercel — falls back to VERCEL_PROJECT_PRODUCTION_URL.
SITE_URL=
